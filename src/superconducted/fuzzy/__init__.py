"""Fuzzy-system layer: membership functions, TSK rule base, defuzzification,
fuzzification placement, output squashing.

The TSK inference math in :mod:`superconducted.fuzzy.tsk` is a locked module.
Modifying it requires an ADR in ``docs/decisions.md`` and sign-off from both
leads.
"""
