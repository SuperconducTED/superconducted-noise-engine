# Numerical Claims Register

## Purpose

Every specific number in public-facing SuperconducTED documentation
(flagship `README.md`, org profile, `docs/architecture.md`, `docs/findings/`,
`docs/state-of-the-project/`, paper drafts, presentation slides) must
trace to a row in this file. Numbers without a row here either get a
row before merge, or get removed.

This file exists because the project shipped a fabricated benchmark
figure (`~0.686% fidelity deviation` attributed to Bautra et al. 2026)
that propagated across four documents over multiple weeks before
verification surfaced the fabrication. The discipline below is designed
to make that failure mode structurally hard to repeat.

## Rules

1. **Every claim has a verifiable source.** Either a published paper
   with arXiv ID / DOI, a file in this repository with a path (and
   line number where useful), a script committed to `scripts/` with a
   recorded output, or a measurement procedure documented enough that
   a reader can rerun it.

2. **"It sounds right" is not a source.** If a number cannot be traced
   to one of the four categories above, it does not belong in a
   public-facing document. Hedged language ("on the order of",
   "roughly") does not exempt a claim from needing a source · it just
   means the source itself was approximate.

3. **`Last verified` is a real date.** Set when someone opened the
   source and confirmed the value. Update when re-verified. If the
   underlying source changes (new measurement supersedes old, paper
   retraction, code refactor changes a calculation), update the value
   and the date together · do not silently keep the old value.

4. **Stale claims get flagged.** Anything not re-verified within
   90 days carries a `STALE` flag in the Notes column. Stale flags
   do not block use of the number, but they require the next person
   touching the claim to re-verify before propagating.

5. **Claims removed from public docs are not removed from this file.**
   They move to the `Retired` section at the bottom with the reason
   recorded. This is the audit trail.

6. **Claims measured from this repository move with this repository.**
   A claim whose source is a command run against this repo — test counts,
   file counts, rule arities — is invalidated by any PR that changes what
   that command returns. The PR that changes it updates the row's value,
   source commit, and `Last verified` date in the same commit. Rule 3
   already implies this; it is stated separately because NC-021 drifted by
   17 across two PRs that had no reason to open this file, and a
   verification runbook citing the stale row reported a regression that did
   not exist (issues #40, #29). Runbooks citing a row of this kind must also
   name the commit it was measured at, so a stale row surfaces as a visible
   mismatch instead of a silent contradiction.

## Active claims

| ID | Claim | Value | Source | Last verified | Notes |
|----|-------|-------|--------|---------------|-------|
| NC-001 | Aer ensemble latency, N=1, shared simulator hoisted, 1024 shots | 0.07s | `docs/findings/aer-integration-walkthrough.md`, commit `4006f6c` | 2026-05-25 | Includes warmup amortization · per-member cost on subsequent members is higher |
| NC-002 | Aer ensemble latency, N=8 | 0.91s | `docs/findings/aer-integration-walkthrough.md`, commit `4006f6c` | 2026-05-25 | |
| NC-003 | Aer ensemble latency, N=16 | 1.89s | `docs/findings/aer-integration-walkthrough.md`, commit `4006f6c` | 2026-05-25 | |
| NC-004 | Per-member cost after warmup amortization | ~0.12s | Derived from NC-002, NC-003 | 2026-05-25 | (0.91 − 0.07) / 7 ≈ 0.12 · (1.89 − 0.07) / 15 ≈ 0.121 |
| NC-005 | Harness validation test count | 15 | `tests/test_metrics.py` | 2026-05-25 | All 15 tests pass · property categories per ADR-022 |
| NC-006 | Harness validated metric properties | 6 | `docs/decisions/drafts/ADR-022-benchmark-validation-criteria.md` | 2026-05-25 | Identity · symmetry/asymmetry · bounds · monotonicity · determinism · reference value |
| NC-007 | KL divergence smoothing epsilon | 1e-12 | `docs/implementations/2026-05-13-harness-metric-sanity-checks.md` | 2026-05-25 | |
| NC-008 | Calibration polling cadence, target | hourly at `:37` | `.github/workflows/calibration-poll.yml`, `docs/implementations/2026-05-13-calibration-poller-cron.md` | 2026-05-25 | Target schedule · not the realized cadence (see NC-009) |
| NC-009 | Calibration polling cadence, sustained measured | ~11.1 snapshots/day | `git ls-tree -r origin/calibration-data` count divided by days since first snapshot, measured 2026-05-25 | 2026-05-25 | Below theoretical max of 24/day due to GitHub Actions scheduler drops |
| NC-010 | Calibration polling cadence, theoretical max | 24 snapshots/day | One snapshot per hour, 24 hours per day | 2026-05-25 | Never observed in practice |
| NC-011 | Snapshot count, measured | 127 | `git ls-tree -r origin/calibration-data` filtered to `snapshots/*.json`, 2026-05-25 | 2026-05-25 | |
| NC-012 | ANFIS training snapshot floor | ≥ 1170 | NC-045, derived as `count_trainable_parameters` × 5 at `main` @ `5f935ea`; the ≥ 630 this row carried until 2026-09-09 came from `docs/decisions.md` ADR-014 context | 2026-09-09 | **Value corrected 2026-09-09 (#56 FR-10).** Was `≥ 630`, from `roughly 126 trainable parameters × 5`; the 126 was never derived by any file and undercounts the consequent term by one output dimension. The measured count is 234, so the floor is 1170 — see NC-045, and NC-046 for how it moves with MF shape. Working minimum · **the unit was ambiguous until 2026-08-30 and is now decided: distinct device states, not snapshot files** (#45; `docs/implementations/2026-08-29-calibration-yield-and-poller-defects.md`) · a samples-per-parameter heuristic counts independent samples, so duplicate rows do not count toward it · **distinct states are an upper bound on independent samples, not a count of them**: consecutive states are temporally correlated (T1/T2 ~daily, readout ~4 h), so the effective sample size is below the distinct-state count, and 630 is a working threshold from the rule of thumb, not a guarantee of training sufficiency; the temporal-dependence assessment is deferred with the trainer (PR #50 review, 2026-09-02) · see NC-025 |
| NC-014 | T1 missingness rate on qubit 72 of ibm_fez | ~0.64% | `tests/fixtures/calibration/ibm_fez_20260513T121322Z_q72_missing_t1t2.json` (1 of 156 qubits affected) | 2026-05-25 | Single snapshot · not a population rate |
| NC-015 | ADR count at end of cycle 1 | 22 | `docs/decisions.md` plus `docs/decisions/drafts/` | 2026-05-25 | ADR-001 through ADR-022 |
| NC-016 | ADR cycle 1 drift count | 2 | `docs/audits/2026-05-25-first-cycle-audit.md` | 2026-05-25 | ADR-006 and ADR-016 |
| NC-017 | Follow-up issues from cycle 1 audit | 8 | `docs/audits/2026-05-25-followup-issues.md` | 2026-05-25 | |
| NC-018 | Default ensemble size | 32 | `docs/decisions/drafts/ADR-021-aer-integration-constraint-and-factory-ensemble.md` | 2026-05-25 | Configurable · 8 used in ADR-019 ablation protocol |
| NC-019 | Default shot count per ensemble member, ablation protocol | 4096 | `docs/decisions/drafts/ADR-019-mf-ablation-methodology.md` | 2026-05-25 | |
| NC-020 | Benchmark circuits in ablation suite | 4 | `docs/decisions/drafts/ADR-019-mf-ablation-methodology.md` | 2026-05-25 | Random Clifford · GHZ · QFT · efficient_su2 |
| NC-021 | Full test-suite size | 465 | `python -m pytest tests/ -q` and `python -m pytest tests/ --collect-only -q -o addopts=""`, both run at `d7ccb8c` on `mert/issue-48-threshold-rows` | 2026-09-10 | Was `149` at `981f324`, then `166` at `1873625`, then `225` at `2af5c16`, then `248`, `266` and `280` across the #55 rounds (see the retired chain in `docs/implementations/2026-09-02-*`), then `352` at `fd9bb0d` and `356` after the #69 review fixes, then `360` at `5397bb6` · `370` was recorded for the merge of `645b4d1` into this branch but was edited **inside** that merge commit, so it could not name the commit it was measured at, which is the whole point of Rule 6; the value itself was correct · `391` adds the 21 PR #70 review-fix tests (11 in `tests/test_pipeline_health.py` for section 9's provenance, commit-on-change, index parsing and floor-configuration checks, 4 in `tests/test_backfill_state_index.py` for `--rebuild`, and the ordering guard) · direct measurement, never a branch-count derivation · **collected size only**: 8 `tests/test_file_snapshots.py` cases fail on Windows for a pre-existing reason that reproduces identically at the merge base `645b4d1`, so the pass count is what CI on `ubuntu-latest` reports, not a local run · cite this row in verification runbooks rather than recalling a count, and state the commit it was measured at · `391` at `e3c4ea8` measured this branch before `main` reached `3b949cd`; the merge at `4bea68c` brought the #56 cycle-2 and training-floor work with 10 more tests, and the floor-configuration guard at `94000a3` adds 1, so the merged tree collects `402` · measured at the merge per Rule 6, never derived by adding branch deltas · `5e49bff` adds the 3 cold-start guard tests, so 402 + 3 = 405 collected, re-measured not derived · `7ec173f` adds the 14 PR #70 review-round-3 tests (4 in `tests/test_canonical_snapshot_digest.py` for the qubit digest's `date` normalisation, the document scope still keeping it, and the two rejected flag combinations; 6 in `tests/test_pipeline_health.py` for the section 9.3 text-bounds and font-size checks and the four `TestHistoricalSweep` cases; 4 in `tests/test_backfill_state_index.py` for fractional-stamp ordering and the three tolerance cases), so 405 + 14 = 419, but the value here is the direct collection at `7ec173f`, not that sum · **this run also passed all 419 on Windows**, where the round-2 note recorded 8 `tests/test_file_snapshots.py` failures: they did not reproduce in a clean interpreter at a short path rather than the repository `.venv`, which confirms that block's reading of them as an environment artefact; `ubuntu-latest` remains the authority · `c966ba4` adds the 4 tests that close issue #48 section 9.2 (3 in `TestEndToEndFixture` for the committed fixture archive, the re-stamped document collapsing to one device state, and byte-identical re-rendering; 1 for the platform-line-ending defect that fixture exposed), so 419 + 4 = 423, but the value here is the direct collection at `c966ba4`, not that sum · `1309335` adds the 4 PR #70 second-reviewer tests (1 in `TestHistoricalSweep` for a recovered state counting on the poll clock but not the device clock, 3 in `TestLedgerParsing` for the now-required `last_update_date` column), so 423 + 4 = 427, but the value here is the direct collection at `1309335`, not that sum · that commit also changes an existing assertion rather than adding one: `polls_yielding_new_state_24h` was counting documents, so `test_metrics_count_distinct_states_and_only_new_ledger_rows` was renamed and its expectation corrected from 2 to 1, which is a fix and not a count change · `2321301` closes the remaining second-review minors and adds 11 tests (4 in `TestOneNameUnderTwoPartitions` for the one document filed under two month partitions, 3 in `TestTrailingWindows` for the sparkline's thirty complete days, 1 for `polls_fired_24h` counting polls rather than ledger rows, and 3 in `TestFloorsAreConfiguration` for the shipped floors bracketing NC-046's registered range, fitting the canvas, and every label naming a register row), so 427 + 11 = 438, but the value here is the direct collection at `2321301`, not that sum · `447` at `416e58fe` is this branch rebased onto `4355850`, adding issue #49's 9 sweep tests (4 in `tests/test_calibration.py` for the `SweepOutcome` contract, 5 in the new `tests/test_calibration_poll_workflow.py` guarding the sweep cron across the three places GitHub forces it to appear) to main's 438 · 438 + 9 is 447, but the value is the direct collection and not that sum · #89 adds no tests, so this count is the same whether or not the branch carries it, which is why the pre-#89 rebase at `7003f947` reached the same number from a different base · re-measured independently at `98fd22b`, after the post-#89 documentation pass that strengthens `test_the_hourly_poll_is_still_scheduled` without adding a test, and returned 447 again · this branch's own pre-rebase `379` at `51140cd` is superseded: it described a tree based on `3b949cd` that nobody will merge, exactly as that entry predicted when it noted PR #70 was concurrently claiming a different value and whichever landed second would re-measure · **all 447 also passed on Windows** in a clean 3.12.10 interpreter at a short path, and the 8 `tests/test_file_snapshots.py` failures recorded pre-rebase did not reproduce there, matching the `7ec173f` note above that reads them as a repository `.venv` artefact rather than a platform one · `ubuntu-latest` remains the authority for the pass count · `465` at `d7ccb8c` adds the 18 issue #48 section 7.3 threshold-evidence tests, all in the new `tests/test_threshold_distributions.py` (2 on hour truncation, 5 on the coverage window arithmetic including the two empty cases, 4 on the nearest-rank percentile, 3 on the strictness of the two share counters, 4 on deriving a first sighting from the earliest stamp rather than from `is_new_state`) · 447 + 18 is 465, but the value is the direct collection at that commit and not that sum · all 465 also pass on Windows in a clean 3.12.10 interpreter at a short path · `ubuntu-latest` remains the authority for the pass count |
| NC-022 | ADR statuses drifted from the 2026-05-25 snapshot table | 4 | Each `ADR Cycle Summary` row in `docs/state-of-the-project/2026-05-25-bootstrap-to-cycle-1.md` compared against its `**Status**` line in `docs/decisions.md` at `dfee09c` | 2026-08-24 | ADR-006, ADR-010, ADR-012, ADR-018 · excludes ADR-019 through ADR-022, which had no ledger entry to drift from at that commit · they were promoted into the ledger by `d7ee18b` and issue #37 is closed; the value stands as measured at `dfee09c` and is not restated for later commits |
| NC-023 | Probability that `consequent_init="random"` yields an identity channel | 1/4 (0.25) | Derivation in ADR-024 (`docs/decisions.md`): rows 0 and 1 of each consequent matrix are disjoint i.i.d. zero-mean draws and weighted-average defuzzification applies one fixed non-negative weighting to both, so the two crisp outputs are i.i.d. zero-mean and P = 1/2 × 1/2 · pinned by `tests/test_channel_viability.py::test_degeneracy_rate_is_one_quarter` | 2026-08-28 | Analytic, not fitted · invariant to rule count, MF placement, and input vector, which move the outputs' variance but not their sign · corroborated at 0.2420 over seeds 0-1999 for both `endpoint` and `interior` placement, and in [0.236, 0.261] for 8/27/64-rule grids under two input vectors · supersedes the "one in four to one in eight" figure in Issue #35, which was an eight-sample artifact |
| NC-024 | Consequent seed-search limit, and the probability of exhausting it by chance | 64 seeds; 4⁻⁶⁴ ≈ 2.9e-39 | `DEFAULT_SEED_SEARCH_LIMIT` in `src/superconducted/integration/aer_factory.py` · exhaustion probability is NC-023 raised to the limit, pinned by `tests/test_channel_viability.py::test_default_seed_search_limit_is_generous` | 2026-08-28 | Sets the threshold at which `first_viable_seed` stops calling a failure bad luck and calls it structural · the limit is generous by design, not tuned to observed data: the shipped 3×3×3 grid is viable at seed 1 (`endpoint`) and seed 0 (`interior`) |
| NC-025 | Distinct device states in the calibration archive | 504 | #45 §7, measured against `calibration-data` @ `f0930b9` (894 snapshot files, 43.6% byte-identical in their qubit block to the previous one) | 2026-08-30 | The unit NC-012 counts as of the 2026-08-30 decision · 504/234 ≈ 2.15 samples per trainable parameter, well below the 5× the floor is derived from (**corrected 2026-09-09, #56 FR-10**: this note read `504/126 ≈ 4.0` until the parameter count was measured rather than recalled — see NC-045; the 504 itself is unchanged) · the gate is **not** met · distinct is not independent: 504 is an upper bound on the independent-sample count (temporal correlation, see the NC-012 caveat), which only strengthens "not met" · **independently reproduced 2026-09-09** by `scripts/backfill_state_index.py` + `scripts/pipeline_health.py` at the same ref, from the committed qubit-block digest definition rather than #45's enumeration: 894 documents, 504 distinct states, idempotent on a second run · that reproduction is issue #48 step 4, the acceptance test for the pipeline-health design, and it is recorded here rather than as its own row because it measures this same quantity at this same ref · **the definition moved 2026-09-10 (PR #70 review round 3) and this reproduction is re-owed under it**: `qubit_digest` now normalises away each parameter record's own `date`, matching the payload digest, so a re-measurement reproducing an identical value is one state rather than two · over 60 consecutive documents at `f0930b9` both definitions give 34 distinct states and no pair merges only under stripping, at 43.3% duplication against this row's 43.6%, so the 504 is expected to stand, but that is a 6.7% contiguous sample, **not** a re-run, and the figure to trust is the one the dispatched `backfill=true` produces at this ref |
| NC-026 | Historical calibration properties, verified retention depth | ≥ 60 days | Actions run [33260786341](https://github.com/SuperconducTED/superconducted-noise-engine/actions/runs/33260786341) via `scripts/probe_historical_properties.py`, channel `ibm_quantum_platform`, plan `open` | 2026-08-30 | Ten depths from 1 to 60 days all returned older documents; none denied · lower bound only, 60 d was the deepest probed · IBM's own API reference claims `NotImplementedError` on this channel and is stale · re-runnable any time via the Calibration Historical Probe workflow · the probe's OK criterion was tightened after this measurement to reject a response NEWER than the instant requested (a service clamping to a retention floor would otherwise score OK); all ten depths in the original run returned documents OLDER than their request, so the claim stands unchanged under the stricter test · 2026-09-02: the probe now exits 1 (indeterminate) rather than 2 when a depth is ERROR/MALFORMED and none is served (PR #50 review); the original run had ten OK rows and no errors, so this too leaves the claim unchanged |
| NC-027 | `init_error` per-qubit coverage after the schema cutover | 116 of 156 qubits | `load_snapshot` on `snapshots/2026-08/ibm_fez/20260828T031723000000Z.json` at `f0930b9`; `scripts/init_error_analysis.py --ref f0930b9` (NC-027 block): 0 of 40 sampled post-cutover snapshots carry the field for all 156 qubits, sample rule `files[::5][:40]` over the 225 post-cutover files | 2026-09-02 | Refines #45 §6b, which established the cutover is clean at *snapshot* granularity — a weaker claim than per-qubit completeness · a fixed set of 40 qubits never reports it, plus 1 intermittent · re-run 2026-09-02 from the committed script: 0 of 40 complete, 40 always missing, 1 intermittent, unchanged |
| NC-028 | `init_error` out-of-sample R² across a 20-day time split | −0.585 | `scripts/init_error_analysis.py --ref f0930b9`: OLS of `init_error` on the six co-observed per-qubit fields over 40 snapshots (`files[::5][:40]` of the 225 post-cutover files under `snapshots/2026-08/ibm_fez/`), n = 4607 qubit-records; split **between snapshots**, first 20 train (08-04→08-19, n = 2300), last 20 test (08-19→08-24, n = 2307); exact value −0.584865 | 2026-09-02 | In-sample R² is 0.139 · negative out-of-sample R² means the fit predicts worse than the held-out mean · this is the evidence that eliminated imputing `init_error` for the 669 pre-cutover snapshots · **independently reproduced by the PR #50 reviewer at −0.584865** · the 2026-08-30 figure came from a row-midpoint cut that lands inside the 2026-08-19T12:45:23 snapshot and is tie-order sensitive (−0.586 in snapshot order; −0.61..−0.58 over 200 orderings of that one snapshot); the snapshot-boundary split is canonical because no ordering can move it · the analysis lived only in a session scratchpad until 2026-09-02 |
| NC-029 | Calibration capture rate over the 2026-08-27..30 scheduler outage | ≤ 19.3% (11 of ≥57) | `docs/evidence/pr47-outage-enumeration/enumeration-2026-08-30.tsv`; Actions run [33301248740](https://github.com/SuperconducTED/superconducted-noise-engine/actions/runs/33301248740) diffed against `origin/calibration-data` filenames | 2026-08-30 | Upper bound on capture, because the 1 h sweep provably undercounts: 2 documents we hold were not returned by it · 46 of those documents were recoverable at measurement time · the sweep's honour check added 2026-09-02 (an answer stamped newer than its request now fails the sweep, PR #50 review) does not affect this figure: every stamp counted lies inside the window and is therefore a document that exists whichever query returned it, and completeness was already stated as a lower bound; the per-query mapping of the 2026-08-30 run was not recorded, so it cannot be re-scored under the check · 2026-09-02: the sweep's recall is now measured at 87.9% (NC-031), which quantifies the undercount this row asserts qualitatively and confirms the ≤ direction · NC-032 shows the shortfall is not confined to outage windows |
| NC-030 | IBM document republication rate, `ibm_fez` | ≥ 19 documents/day (mean interval ≤ 1.26 h) | Same enumeration as NC-029: ≥57 distinct `last_update_date` values in a 72 h window | 2026-08-30 | **Not** the same quantity as the ~4.0 h readout-parameter cadence in #45 §3 — `last_update_date` advances whenever any part of the document changes, gate data included · lower bound · this figure refutes "above ~8 polls/day the extra polls are pure duplicates" at the document level, but says nothing about distinct device states (NC-025) · see the NC-029 note on the 2026-09-02 honour check, which leaves this lower bound unchanged |
| NC-031 | Historical enumeration sweep recall, 1 h step | 87.9% (29 of 33) | `docs/evidence/aug-gap-enumeration/control-2026-08-12_14.tsv`; Actions run [33669739561](https://github.com/SuperconducTED/superconducted-noise-engine/actions/runs/33669739561) swept 2026-08-12T06:00..08-14T20:00 and its returned stamps were differenced against the `calibration-data` filenames for that window | 2026-09-02 | The first direct measurement of the instrument NC-029 and NC-030 are built on · 4 documents the archive provably holds were not returned, so the method **under**-reports · that error direction is why a sweep returning nothing cannot by itself certify an absence, and why the 2026-08-07..17 conclusion rests on NC-033 and NC-034 rather than on the sweeps alone · measured in a high-churn stretch; the aliasing mechanism (a document superseded before the next query lands) is unavailable in a quiet window, so recall there is expected to be higher and is not measured |
| NC-032 | Calibration capture rate outside a known outage, 2026-08-12..14 | ≤ 100% (50 of ≥ 50) | The NC-031 enumeration plus the #53 backfill: 43 documents served inside the window, 4 held-but-unserved, and 3 more recovered that the 1 h sweep never served = ≥ 50 proven to exist; `git ls-tree -r --name-only <remote>/calibration-data -- snapshots/2026-08/ibm_fez/` filtered to `20260812T063125Z .. 20260814T174409Z`, at `calibration-data` `3d1569d`, holds 50 | 2026-09-07 | Was `≤ 70.2% (33 of ≥ 47)` at 2026-09-02, measured before the #53 backfill, when the 14 `MISSED` rows were absent from every month directory on the branch and recoverable; all 14 were recovered on 2026-09-06 · **the current bound is vacuous and must not be read as "capture is fine"** — holding every document you can prove exists gives 100% by construction, and this window is complete only because #53 backfilled the very window the row measures, so it no longer describes what the poller captures during ordinary operation, which is what the 2026-09-02 figure measured · the `≥ 47` denominator was itself an undercount: 3 documents inside the same span (`20260812T081604000000Z`, `20260812T104835000000Z`, `20260812T183935000000Z`) were recovered that the 1 h sweep never served, so that sweep served 43 of the 50 now known to exist, 86.0%, corroborating NC-031's 87.9% from an independent direction · still an upper bound, because the sweep undercounts (NC-031) and nothing proves 50 is the whole population · this window was **not** a flagged outage — it is ordinary operation, so the ≤ 19.3% of NC-029 is not the whole story and the shortfall was not confined to scheduler failures · a capture rate describing ordinary operation now needs a window that has not been backfilled (#54) · see Rule 6 · cite this row with the `calibration-data` commit it was measured at |
| NC-033 | Bulk calibration rounds inside the 2026-08-07..17 polling gaps | 13 rounds · 9,807 parameter measurements | `docs/evidence/aug-gap-enumeration/calibration-rounds.tsv`: per-parameter `date` fields in the three gap-closing snapshots (`20260810T140235Z`, `20260812T055918Z`, `20260817T065740Z`) clustered at a 20-minute gap, keeping rounds of ≥ 300 parameters | 2026-09-02 | Refutes the "backend was idle" half of #45 §5's explanation (a): the device recalibrated throughout the gaps · control: the identical procedure on five normal-period snapshots yields 25 rounds, **25 of 25** matched by an archived document within [−5, +75] min, usually to the second · a **floor**, since a cumulative document keeps only the newest measurement per parameter, so a fully overwritten round is invisible · thresholds (20 min, 300 parameters) are descriptive, not fitted |
| NC-034 | Live-path document stability across the 2026-08-07..17 gaps | 1 distinct `properties` block per window over 154 revisions | `docs/evidence/aug-gap-enumeration/live-path-stability.tsv`: every commit to `calibration-data` inside each window, hashing `properties` of that window's edge snapshot at each revision | 2026-09-02 | Refutes #45 §5's explanation (b) with first-party evidence: the poller received the **same** document 154 times, not fresh ones it then lost · independent of the historical index, because the live call is `backend.properties()` with no `datetime` and never builds an `updated_before` query · only `target` churned between revisions, which is the ordering defect #46 fixed · note 70/30/55 commits here counts a different quantity from #45 §4's 58/35/46 successful runs; neither figure should be cited as the other |
| NC-035 | `ibm_fez` single-qubit `sx` gate length | 24 ns | `git show origin/calibration-data:snapshots/2026-05/ibm_fez/20260513T121322000000Z.json` and `.../20260513T143529000000Z.json`, each filtered to one-qubit `sx` `gate_length` values; archive ref `1accf05`; fixture at candidate `37e1ba0` | 2026-09-07 | Both snapshots contain 156 one-qubit `sx` records and the sole observed value is 24 ns |
| NC-036 | Derived amplitude-plus-phase damping versus Aer maximum SuperOp entry difference | $9.992007221626409 \times 10^{-16}$ | `tests/training/test_aer_pin.py`, all four T1/T2 cases at 24 ns and 60 ns, candidate `37e1ba0` | 2026-09-07 | Below $10^{-12}$ acceptance limit; Qiskit 2.4.1 and Qiskit Aer 0.17.2 |
| NC-037 | Trainable parameter count for 3x3x3 Gaussian TSK grid, output dimension 2 | 234 | `tests/training/test_parameters.py`, candidate `37e1ba0` | 2026-09-07 | 216 consequent and 18 unique premise parameters |
| NC-038 | Gate-bearing `ibm_fez` fixture contents | 688,636 bytes; 156 `sx`; 352 `cz`; 155 usable targets; 1 `t1_missing` | `tests/fixtures/calibration/ibm_fez_20260513T121322Z_with_gates.json`, SHA-256 `02d27ff1bf6af8bb06e0bce886454160926cb3adc1466e536245e03431487082`, candidate `37e1ba0` | 2026-09-07 | `cz` records are deliberately excluded from the one-qubit target parser; skipped qubit is index 72 |
| NC-039 | `ibm_fez` mean-of-targets versus target-at-mean-features gap at 24 ns | $(\Delta\gamma, \Delta\lambda)=(-1.8032599909555808 \times 10^{-5}, -3.7973319935037924 \times 10^{-4})$; max absolute gap $3.7973319935037924 \times 10^{-4}$ | `feature_target_fn(BasicCalibrationVectorizer().extract(snapshot), t_seconds=24e-9) - snapshot_target(qubit_targets(...)).mean` over the NC-038 fixture, candidate `37e1ba0` | 2026-09-07 | Confirms the nonlinear map must not be evaluated at mean features for the snapshot target |
| NC-040 | `ibm_fez` device-versus-target magnitudes at 24 ns | `sx gate_error` median $3.110293362374808 \times 10^{-4}$; readout-error median $2.191162109375 \times 10^{-2}$; mean $\gamma=1.7266737044123665 \times 10^{-4}$; mean $\lambda=6.630210259092216 \times 10^{-4}$ | NC-038 fixture, filtered finite per-qubit values and `snapshot_target(qubit_targets(...))`, candidate `37e1ba0` | 2026-09-07 | 156 finite readout-error values; readout error is outside the current single-qubit channel target |
| NC-045 | Trainable parameters of the TSK rule base in use, and the training floor derived from it | 234 parameters; floor $234 	imes 5 = 1170$ | `count_trainable_parameters` (`src/superconducted/training/parameters.py`) on `TSKRuleBase.from_grid` with three `GaussianMF` per input over three inputs and `output_dim = 2`, run at `main` @ `5f935ea` | 2026-09-09 | **Supersedes the `roughly 126 trainable parameters x 5` clause of NC-012**, whose 126 no reachable file derives: `git log -S'126'` puts it into `docs/architecture.md` on 2026-05-07, before the 27-rule grid existed, and it is reproducible only as one output dimension of consequents plus the premise count ($108 + 18$) . The 234 reproduces NC-037 exactly, which is the check that this measurement uses the same procedure as the registered count . Composition: 216 consequent entries ($k R (d+1) = 2 	imes 27 	imes 4$) plus 18 unique premise parameters ($U p = 9 	imes 2$); `from_grid` shares one MF object across every rule naming it, so 27 rules and 81 antecedent references carry only 9 distinct objects . **Measured against the configuration in use, not an ADR-009-decided one**: ADR-009 reads `Open` at this commit, so if the type or shape changes the floor moves with it, by the amounts in NC-046 . The $	imes 5$ is NC-012's samples-per-parameter rule of thumb and is unchanged . NC-012's caveat carries over undiminished: distinct device states are an upper bound on independent samples, so crossing this floor is a claim gate, not a proof of training sufficiency . Provisional laptop measurement; canonical verification is the batch-2 record per `docs/team.md` |
| NC-046 | Trainable parameter count across all seven membership-function shapes, 3x3x3 grid at `output_dim = 2` | 234 to 252; floor 1170 to 1260 | `count_trainable_parameters` run once per shape at `main` @ `5f935ea`; procedure and per-shape table in `docs/implementations/2026-09-09-training-floor-derivation.md` | 2026-09-09 | One row per reported conclusion per the `## Adding a claim` granularity rule, not one per shape . `GaussianMF` and `TanhSigmoidMF` 234 (2 parameters per MF); `TriangularMF`, `TanhBellMF` and `IntervalGaussianMF` 243 (3); `TrapezoidalMF` and `TanhMF` 252 (4) . The consequent term is fixed at 216 for every shape, so the whole spread comes from the premise term $U p$ with $U = 9$ . **Consequence for ADR-009**: the Interval Type-2 shape (`IntervalGaussianMF`) costs 9 more parameters than the `GaussianMF` baseline, 3.8%, not the doubling the standard objection to IT2 assumes, because IT2 adds one parameter per unique MF object while the consequent term dominates . The `GaussianMF` entry reproduces NC-037 . Provisional laptop measurement |
| NC-047 | Pipeline-health backfill at `calibration-data` @ `c63ce21` | 563 distinct states from 994 documents; 43.4% duplication | At `calibration-data` @ `c63ce21`, written by the Calibration Pipeline Health workflow (run [34454835792](https://github.com/SuperconducTED/superconducted-noise-engine/actions/runs/34454835792), `backfill=true`). Reproducible over a checkout of that ref with `PYTHONPATH=. python scripts/backfill_state_index.py --root <checkout>` then `PYTHONPATH=. python scripts/pipeline_health.py --root <checkout> --floor NC-012=1170`; the second command is what computes the duplication ratio (`pipeline_health.py:360`), so the ref plus these two commands stand on their own if the Actions log expires. That second command also prints `projected_days` and `projected_date`; ADR-025's amendment makes those non-registrable extrapolations, so do not carry them into this file | 2026-09-10 | Candidate floors remain configuration, not a determination of training sufficiency; the commands operate only on the specified committed checkout. Renumbered from NC-042 before merge because PR #68 claims NC-042 for a different statistic and open PRs #87/#56 claim NC-045 and NC-046. · **Provisional**, measured on the `ubuntu-latest` GitHub Actions runner. `docs/team.md` puts canonical verification in @BurakOztekin's desktop batch record and says runs on any other machine stay provisional and are cited with the machine named; an Actions runner is any other machine, so this row carries the same provisional mark NC-045 and NC-046 do. · **Composition at `c63ce21`**: 994 documents, 563 distinct `qubit_digest` values, 43.3601609658% duplication rendered as 43.4%; `snapshots/` holds 994 files at that ref, so `documents_total` is the whole archive and not a subset. The 563 first sightings are derived from `last_update_date` order, which is what ADR-025's amendment requires, **not** from the `is_new_state` column, which that amendment declares non-authoritative about chronology once a sweep has appended out of order. · **Superseded reading**: 537 distinct states from 936 documents, 42.6% duplication, at `46f93c8`, measured locally and under the pre-2026-09-10 digest definition, which hashed each parameter record's own `date`; see the NC-025 note for that definition change. **That qualifier belongs to the old reading only**: `c63ce21` was produced by the date-stripped digest PR #70 merged. · **This row is a dated anchor, not a live figure.** The archive grows hourly and no PR invalidates this row, so Rule 6's mechanism does not reach it; the live value is `health/metrics.json` on `calibration-data`, regenerated on every render. Cite this row only as the state at `c63ce21`. · **Claim renamed twice**: from `Current pipeline-health backfill`, because a name saying *current* is stale within the hour, and then from `Pipeline-health backfill, first workflow-produced index`, because *first* is a second claim embedded in a name and is false unqualified: `calibration-data-health-validation` @ `034a3d8` carries an earlier workflow-written index (2026-09-09 20:35:31Z, `github-actions[bot]`, 991 documents, 562 states, 43.2896%), on a separate branch that is not an ancestor of `calibration-data`. The ref in the name carries the same information and needs no separate verification (PR #91 review, @bengisucvd) |
| NC-048 | `ledger_hour_coverage_72h`, measured distribution under the degraded scheduler | 22.2% to 31.9%, median 27.8%, over 125 windows | `python scripts/threshold_distributions.py --root <calibration-data checkout>`, run at `calibration-data` @ `9d7e74c` | 2026-09-10 | The base measurement issue #48 §7.3's coverage thresholds are judged against, recomputed exactly as `pipeline_health.build_metrics` computes the live figure: an hour counts when at least one ledger row falls inside it, and the window is the 72 hours before an hour boundary · rolled one hour at a time across the ledger span, counting only windows lying wholly inside it, because a window reaching back past the first ledger row reads low for reasons that have nothing to do with the scheduler · **the sample is 8.2 days, not four months.** `ledger/` begins at 2026-09-02T16:49Z, when ADR-025 landed, and is the only poll-time record in this repository that counts no-op polls; `calibration-data`'s own commit history reaches only 2026-08-01 and pre-ledger records only polls that produced a document · 54 of the span's 196 hours are covered, which is what a poller firing about 6.1 times a day looks like (NC-009 and the #49 measurement) · **contains no scheduler collapse**, so it describes degraded-normal operation and nothing else; the 2026-08-27..30 outage (NC-029) predates the ledger entirely |
| NC-049 | Firing rate of issue #48 §7.3's three coverage thresholds, against NC-048 | below 15%: 0 of 125 (0.0%) · below 50%: 125 of 125 (100%) · below 75%: 125 of 125 (100%) | `python scripts/threshold_distributions.py --root <calibration-data checkout>`, run at `calibration-data` @ `9d7e74c` | 2026-09-10 | A threshold asserts how often it will fire, so it is a claim and needs this row; §7.3 decision item 4 says so and notes that none of its numbers had one · **§7.3's own figures do not reproduce.** It reported 399 windows, a maximum ever observed of 83.3%, p95 of 81.9%, red below 15% firing on 3.3% and the 75% target firing on 85.5%. Measured here: 125 windows, maximum 31.9%, p95 30.6%, red firing on 0.0% and 75% on 100% · **the median reproduces and the tail does not**, which locates the error precisely. Three sources were checked: the live ledger at `9d7e74c` (125 windows), the ledger on `calibration-data-health-validation` @ `49c3c59` (99 windows), and IBM's `last_update_date` clock over the whole backfilled archive as a proxy for publication rather than polling (2816 windows). Both ledgers give p50 **27.8%**, exactly the p50 §7.3 reports, so its median came from real ledger data. None of the three yields its p95 of 81.9% or its maximum of 83.3%: the two ledgers top out at 31.9% and the proxy at 73.6% · the tail is the half the thresholds actually depend on, since 15% and 75% are both drawn from it · so the coverage half of §7.3 rests on a distribution this repository cannot produce, and the decision it supports should be re-taken against this row · **the 15% red is unevidenced, not disproven**: the ledger holds no collapse, so what fraction of collapse-windows it would catch is unmeasured, and 0 of 125 only says it never fires during degraded-normal operation, which is the behaviour an alarm wants · amber below 50% is not an alarm at all: it is true in every window measured, which is the concern §7.3 raised about itself in its closing line and understated as chronic rather than constant |
| NC-050 | Interval between consecutive first sightings of a distinct device state | median 3.94 h, p95 11.01 h, maximum 82.68 h, over 565 intervals | `python scripts/threshold_distributions.py --root <calibration-data checkout>`, run at `calibration-data` @ `9d7e74c` | 2026-09-10 | What `hours_since_last_new_state` samples, and the base measurement for the staleness bounds in NC-051 · first sighting is the earliest `last_update_date` carrying a digest, derived rather than read from the index's `is_new_state` column, matching `pipeline_health.first_sightings` and for its stated reason: that column is decided by append order and a sweep files documents out of order · unlike NC-048 this rests on the **whole backfilled archive**, 2026-05-13 to 2026-09-10, because the state index was populated from `snapshots/` rather than from poll rows · the 82.68 h maximum is the 2026-08-27..30 scheduler outage (NC-029), so the tail is real history and not an artefact · 565 intervals against the 566 distinct states in the index at this ref |
| NC-051 | Exceedance of issue #48 §7.3's staleness bounds, against NC-050 | over 12 h: 23 of 565 (4.1%) · over 24 h: 7 of 565 (1.2%) | `python scripts/threshold_distributions.py --root <calibration-data checkout>`, run at `calibration-data` @ `9d7e74c` | 2026-09-10 | **§7.3's staleness figures reproduce exactly.** It reported 23 of 561 above 12 h and 7 of 561 above 24 h with a maximum of 82.68 h; the counts and the maximum are identical here and only the denominator moved, from 561 to 565, because the archive gained four states since · that is the check the coverage half fails in NC-049, and it is why these two bounds can be adopted and those three cannot · **the code does not implement the 12 h amber.** §7.3 states that `STALENESS_BANDS` in `scripts/pipeline_health.py` already buckets at 24 and 72 h so the code and the decision agree; the 24 h bound does agree, but the constant is `(24, 72, 168)` and carries no 12 h boundary, so an amber at 12 h is not representable without a new band · nothing colours against either bound yet, so this costs nothing today and is recorded before the first one is wired |

## Retired claims

| ID | Claim | Value | Retired | Reason |
|----|-------|-------|---------|--------|
| NC-R001 | Bautra et al. 2026 fidelity deviation vs. real hardware | ~0.686% | 2026-05-25 | Fabricated number. Bautra et al. 2026 (arXiv:2603.14607) uses Weighted Jaccard Similarity in the 43-97% range, not fidelity deviation. No source ever existed. Removed from flagship README, org profile, and `docs/architecture.md`. |
| NC-R002 | 630-snapshot floor projected date | ~2026-07-10 | 2026-08-30 | Superseded twice over. The projected date passed without the gate being met in any information-based unit, and the projection was built on the snapshot-count unit that #45 argues against and that NC-012 has now replaced with distinct device states (NC-025). Its inputs NC-009 and NC-011 are both 2026-05-25 measurements long overtaken. Re-derive from NC-025 if a projection is needed. The three documents still carrying `~2026-07-10` — `docs/advisor/2026-05-25-questions-for-akba.md`, `docs/findings/2026-05-25-empirical-synthesis.md`, `docs/roadmap/2026-05-25-cycle-2-plan.md` — are dated snapshots and are deliberately **left unedited**: they were accurate on 2026-05-25, and dated docs are reconciled by appending an as-of section, never by rewriting rows in place. |

## Adding a claim

1. Find or open a verifiable source.
2. Pick the next available `NC-NNN` ID.
3. Add a row in `Active claims` with claim, value, source path, today's
   date, and any necessary notes.
4. The claim is now safe to cite in public-facing documentation.

### Granularity for a results or ablation table

The unit of registration for a results table is **one row per reported
conclusion, not one row per cell.**

Such a row must cite three things: the committed results TSV **by path**, the
**archive ref** the run consumed, and the **commit the table was produced at**.
The TSV is the audit trail for the individual figures. A reader who wants one
cell reads the TSV at that commit; they do not look for a register row that
names it.

The reason is arithmetic. A seven-shape ablation over four circuits and four
metrics is a few hundred figures, and the ADR-019 ablation ticket priced a
strict per-figure rule at 500-600 rows. A register that long is a register
nobody reads, which defeats the purpose of having one.

The counter-rule that keeps this honest: **a figure quoted in prose outside the
table still needs its own row**, or an explicit "to be measured". The
conclusion-row rule changes the unit of registration for tables. It does not
create an exemption from Rule 3 or from the obligation that every number in a
public-facing document resolves to a row or to a named file on `main`.

## Retiring a claim

1. Move the row to `Retired claims`.
2. Add the retirement date and the reason (`superseded`, `fabricated`,
   `source no longer accessible`, `claim restructured into NC-XXX`).
3. Grep public-facing documentation for the value and citations of the
   retired claim. Remove or replace.

## Re-verification

A reasonable rhythm: re-verify the table at the start of every
development cycle. Update `Last verified` dates for claims that still
hold against current sources. Move stale or superseded claims to
`Retired`. This becomes part of the cycle-open process the same way
the cycle-1-close audit was.
